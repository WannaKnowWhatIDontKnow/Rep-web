# 의존성 파일 (용량이 매우 큼)
/node_modules

# 빌드 결과물
/build
/dist

# 로그 파일
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 환경 변수 파일 (API 키 등 민감 정보 포함 가능)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# 운영체제 생성 파일
.DS_Store
Thumbs.db
